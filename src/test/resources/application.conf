# The retry wait is short here so the real retry path runs in a test's lifetime; the
# arithmetic over the base is the same code the five-second default uses.
linkwarden.retry.base-delay = 200ms
linkwarden.retry.max-attempts = 4
